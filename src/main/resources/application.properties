spring.application.name=gengshop

# Port untuk Spring Boot
server.port=8080

# Database Connection Settings
spring.datasource.url=jdbc:postgresql://localhost:5432/gengshop
spring.datasource.username=gengshopuser
spring.datasource.password=password

# JPA/Hibernate Settings
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Bagus untuk tengok SQL query waktu dev
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# JWT Configuration
# Generate secret key guna: openssl rand -base64 32
#application.security.jwt.secret-key=lhUU9sALr/2YdPtXtUpsG969pQ5xjb0KMurVygQR0is=

# Access token expiration (15 minutes - recommended untuk production)
# application.security.jwt.expiration=900000

# Alternative: 1 hour
# application.security.jwt.expiration=3600000


